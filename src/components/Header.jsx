import React from 'react';
import Nav from '../../src/components/Nav';

export function Header() { /* header class renders the nav component */
return (
    <div className="container bg-info">
    <Nav />
    </div>
);
}